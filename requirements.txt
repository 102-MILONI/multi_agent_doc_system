# Core ML and LLM support
transformers
torch

# Streamlit frontend UI
streamlit

# File parsing and data processing
PyPDF2
pandas
requests
tqdm

# Optional utilities
rich            # for better terminal output formatting
python-dotenv   # for environment config (if used)

# Testing
pytest